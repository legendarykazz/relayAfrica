{
  "name": "relayafrica-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w relayafrica-web",
    "dev:api": "npm run dev -w relayafrica-api",
    "build:web": "npm run build -w relayafrica-web",
    "build:api": "npm run build -w relayafrica-api"
  }
}
